"use strict";
//What if we want to update both the Analytics and app TS files
// - Lets say the analytics.ts is sending info to our HTML
console.log("Sending .....");
/*
using tsc --init will create a tsconfig.json file which
tells TS that this porject is managed by TS
  - NOW we can type tsc and TS will compile all TS file in the project
  - We can enter watch mode for the whole project with tsc -w
  */
